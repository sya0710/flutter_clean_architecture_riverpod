import 'dart:ui';

class LangCode {
  static const String vi = 'vi';
  static const String en = 'en';
}

class LocaleConstants {
  static const Locale vietnamese = Locale(LangCode.vi);
  static const Locale english = Locale(LangCode.en);
}
